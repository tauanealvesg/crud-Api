# crud-Api

editar packed.json "scripts" :

 "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }

npm install express

npm i axios

npm i nodemon
